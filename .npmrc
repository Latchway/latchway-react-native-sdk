save-exact=true
strict-peer-dependencies=true
# React Native's CocoaPods framework exclusions inspect a conventional
# node_modules tree. pnpm's isolated symlink paths make overlapping Fabric
# headers appear as duplicate framework outputs, so native hosts use hoisting.
node-linker=hoisted
