name: Publish React Native SDK v1.0.0

on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

concurrency:
  group: latchway-react-native-v1.0.0-publication
  cancel-in-progress: false

env:
  NODE_VERSION: 24.19.0
  NPM_VERSION: 11.6.2
  PNPM_VERSION: 10.15.0
  RELEASE_TAG: v1.0.0
  RELEASE_VERSION: 1.0.0

jobs:
  publish:
    name: Pack, publish, and release v1.0.0
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    environment: single-maintainer-v1
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@d15e628ca66d93ee5f352c71671a7bc6a97af5c9 # v6.0.8
        with:
          version: ${{ env.PNPM_VERSION }}
          run_install: false
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          registry-url: https://registry.npmjs.org
      - name: Install packaging tools
        run: |
          npm install --global "npm@$NPM_VERSION"
          pnpm install --frozen-lockfile
      - name: Build and pack v1.0.0
        run: |
          pnpm build
          mkdir -p "$RUNNER_TEMP/release"
          pnpm pack --pack-destination "$RUNNER_TEMP/release"
      - name: Publish v1.0.0 with trusted npm publishing
        run: >-
          npm publish
          "$RUNNER_TEMP/release/latchway-react-native-$RELEASE_VERSION.tgz"
          --access public --provenance --tag latest
      - name: Publish the GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$RELEASE_TAG" \
            "$RUNNER_TEMP/release/latchway-react-native-$RELEASE_VERSION.tgz" \
            --repo "$GITHUB_REPOSITORY" \
            --target "$GITHUB_SHA" \
            --title "Latchway React Native SDK $RELEASE_VERSION" \
            --notes "Latchway React Native SDK $RELEASE_VERSION."
